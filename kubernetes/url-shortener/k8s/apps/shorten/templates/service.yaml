apiVersion: v1
kind: Service
metadata:
  name: shorten
spec:
  type: {{ .Values.service.type }}
  selector:
    app: shorten
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
