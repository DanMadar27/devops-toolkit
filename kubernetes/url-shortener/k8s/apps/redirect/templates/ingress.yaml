apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: redirect
spec:
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: redirect
                port:
                  number: {{ .Values.service.port }}
