[defaults]
# Disable SSH host key verification — avoids "Host key verification failed" on first connect.
# Safe for ephemeral EC2 instances where the host key changes on each provision.
host_key_checking = False
